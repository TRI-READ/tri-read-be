# Copy this file to application-secret.yml and fill in local-only secrets.
# application-secret.yml is ignored by Git and is not packaged into the JAR.
app:
  auth:
    # Existing account to promote to the first application administrator at startup.
    bootstrap-admin-login-name: your-login-name
  quiz-generation:
    gemini:
      api-key: ""
