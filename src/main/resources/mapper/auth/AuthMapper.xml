<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triread.api.auth.AuthMapper">

    <select id="findByLoginName" resultType="com.triread.api.auth.AuthUser">
        SELECT id,
               login_name,
               display_name,
               pin_hash,
               app_role,
               enabled
        FROM app_users
        WHERE login_name = #{loginName}
    </select>

    <insert id="insert"
            parameterType="com.triread.api.auth.AuthUser"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO app_users (
            login_name,
            display_name,
            pin_hash,
            app_role,
            enabled
        )
        VALUES (
            #{loginName},
            #{displayName},
            #{pinHash},
            #{appRole},
            #{enabled}
        )
    </insert>

    <update id="updateLastLoginAt">
        UPDATE app_users
        SET last_login_at = #{lastLoginAt}
        WHERE id = #{userId}
    </update>

</mapper>
